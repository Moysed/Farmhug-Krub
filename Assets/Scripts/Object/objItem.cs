using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class objItem : MonoBehaviour
{
    //public scarecrowInfo scarecrow;

    //FarmManager farm;

    void Start()
    {
        //farm = FindObjectOfType<FarmManager>();
    }

    /*public void BuyPlant()
    {
        if(Inventory.singleton.coin >= scarecrow.price)
        {
            Inventory.singleton.coin -= scarecrow.price;
            Debug.Log("Buy : scarecrow");
            //farm.SelectPlant(this);
            //GroundMangement.singleton.closePanel();
            //GroundMangement.singleton.tempPlanting();
        }
        else
        {
            Debug.Log("Not Enough Coin");
        }
        
    }*/
}
